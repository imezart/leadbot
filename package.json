{
  "name": "leadbot",
  "version": "0.1.0",
  "description": "Telegram bot that collects leads for small businesses.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "node --watch --env-file=.env index.js"
  },
  "dependencies": {
    "@grammyjs/conversations": "^2.1.1",
    "grammy": "^1.42.0"
  }
}
